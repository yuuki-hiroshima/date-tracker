
# ========== 完成イメージ ==========

# 入力：
#     ・コマンド引数で受け取る
#     ・名前と日付は必須（前後の空白は削除）
#     ・欠けている or 日付形式が違うなら、エラーメッセージを表示し終了

# 処理：
#     ・JSONを読み込む（無ければ空）（理由：読み取りは1つに絞ると単純化、保存は両方で互換を保つ）
#     ・入力を検証する
#     ・辞書に上書き登録
#     ・JSONとCSVの両方へ保存

# 出力：
#     ・「登録：<名前>=<日付>」のように1行で確定結果を表示

# エラー対応：
#     ・処理を中断しメッセージを表示（終了コードは気にしなくてOK：将来sys.exit(1)にする余地あり）

# 補足：
#     前後だけ削除（strip()）にするか、中の空白も全削除にするか（"".join(s.split())）はお好み。

# ========== 擬似コード ==========

入力：
    プログラム開始時にコマンド引数から name, date を受け取る
    前後の空白を削除 → 空ならエラー終了
    日付形式をチェック（YYYY-MM-DD）→ 不正ならエラー終了

処理：
    load_json で既存データ取得 → 上書き登録 → save_json / save_csv

出力：
     「登録：name = date」 を表示

エラー対応：

補足：


# ========== Pythonコード ==========

import argparse
from app import is_iso_date, load_csv, load_json, save_csv, save_json, days_between

# ==========  ==========



# ==========  ==========



